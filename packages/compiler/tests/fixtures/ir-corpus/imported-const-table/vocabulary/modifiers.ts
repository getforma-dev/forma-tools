// Two properties per row, one read in child position and one in an attribute,
// so a substitution that reaches only one of the two is visible in the golden.
export const MOD_KEYS = [
  { k: 'Shift', code: 'shift' },
  { k: 'Ctrl', code: 'ctrl' },
];
